{
  "items": [
    {
      "title": "{{ index_name }} Index for FTD {{ ftd_version }}",
      "content": "index.md"
    }{{ "," if index_list }}
{% for item in index_list | sort %}
    {
      "title": "{{ item }}",
      "content": "{{ item | camel_to_snake }}.md"
    }{{ "," if not loop.last }}
{% endfor %}
  ]
}
