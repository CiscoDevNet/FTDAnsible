NetworkObject:
  value: "192.168.2.0/24"
  subType: "NETWORK"
  dnsResolution: "IPV4_AND_IPV6"

NetworkObjectGroup:
  objects:
    - "{{ networkObject }}"

AccessRule:
  parentId: "default"
  sourceNetworks:
    - "{{ networkObject }}"
